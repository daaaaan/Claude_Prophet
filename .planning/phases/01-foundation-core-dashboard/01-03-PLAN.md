---
phase: 01-foundation-core-dashboard
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - dashboard/static/templates/dashboard.html
  - dashboard/static/js/app.js
  - dashboard/static/css/output.css
  - dashboard/ticker.go
  - dashboard/events.go
  - cmd/bot/main.go
autonomous: false

must_haves:
  truths:
    - "User can see a reverse-chronological feed of recent orders, fills, and position state changes that updates live via WebSocket"
    - "User can see connection status indicator with green/yellow/red color degradation based on data age (green < 5s, yellow 5-30s, red > 30s)"
    - "User can see bot health indicator showing alive/dead status and last activity timestamp"
    - "Activity feed updates in real-time without page refresh"
  artifacts:
    - path: "dashboard/static/templates/dashboard.html"
      provides: "Activity feed panel, connection status indicator, bot health indicator -- all integrated into existing layout"
      contains: "activities"
      min_lines: 120
    - path: "dashboard/static/js/app.js"
      provides: "Alpine.js store with activity formatting helpers and time-ago computation"
      contains: "formatTimeAgo"
      min_lines: 60
    - path: "dashboard/events.go"
      provides: "ManagedPositionData struct for position state changes in activity feed"
      contains: "ManagedPositionData"
  key_links:
    - from: "dashboard/static/templates/dashboard.html"
      to: "Alpine.store('dashboard').activities"
      via: "x-for loop rendering activity items"
      pattern: "x-for.*activities"
    - from: "dashboard/static/templates/dashboard.html"
      to: "Alpine.store('dashboard').botHealth"
      via: "x-text directives for alive status and uptime"
      pattern: "\\$store\\.dashboard\\.botHealth"
    - from: "dashboard/static/templates/dashboard.html"
      to: "Alpine.store('dashboard').connectionStatus"
      via: "Reactive color classes on status dot"
      pattern: "connectionStatus.*live"
    - from: "dashboard/ticker.go"
      to: "services.ActivityLogger + database.LocalStorage"
      via: "GetCurrentLog for activities, GetAllManagedPositions for position state changes"
      pattern: "(GetCurrentLog|GetAllManagedPositions)"
---

<objective>
Complete the dashboard by building the activity feed panel (showing recent orders, fills, and position state changes in reverse-chronological order), the connection status indicator (green/yellow/red based on data age), and the bot health indicator (alive/dead with uptime). Enrich the Ticker to include managed position state data in snapshots. Run a final visual verification checkpoint.

Purpose: The activity feed and status indicators are the final pieces needed for the user to have full situational awareness. Without the activity feed, the user cannot see what the bot has been doing. Without status indicators, the user cannot trust whether the data is current.

Output: A complete, working Phase 1 dashboard with all panels populated and live-updating.
</objective>

<execution_context>
@/home/dan/.claude/get-shit-done/workflows/execute-plan.md
@/home/dan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-core-dashboard/01-RESEARCH.md
@.planning/phases/01-foundation-core-dashboard/01-01-SUMMARY.md
@.planning/phases/01-foundation-core-dashboard/01-02-SUMMARY.md
@dashboard/static/templates/dashboard.html
@dashboard/static/js/app.js
@dashboard/events.go
@dashboard/ticker.go
@services/activity_logger.go
@models/models.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build activity feed panel, status indicators, and enrich Ticker data</name>
  <files>
    dashboard/events.go
    dashboard/ticker.go
    dashboard/static/js/app.js
    dashboard/static/templates/dashboard.html
    dashboard/static/css/output.css
    cmd/bot/main.go
  </files>
  <action>
**dashboard/events.go** -- Add managed position data to snapshot:
- Add `ManagedPositionData` struct: `PositionID string`, `Symbol string`, `Status string` (PENDING/ACTIVE/PARTIAL/CLOSED/STOPPED_OUT), `Side string`, `EntryPrice float64`, `StopLoss float64`, `TakeProfit float64`, `UnrealizedPL float64`, `CreatedAt time.Time` -- all with json tags
- Add `ManagedPositions []*ManagedPositionData` field to `DashboardSnapshot` with json tag `"managed_positions"`

**dashboard/ticker.go** -- Enrich snapshot with managed positions:
- Add `storage *database.LocalStorage` field to `Ticker` struct (import `prophet-trader/database`)
- Update `NewTicker` constructor to accept `storage *database.LocalStorage` parameter
- In `publishSnapshot`, call `storage.GetAllManagedPositions("")` to get all managed positions. Convert `[]*models.DBManagedPosition` to `[]*ManagedPositionData`. Include in snapshot.
- Update `cmd/bot/main.go` to pass `storageService` to `NewTicker` (the storageService is already a `*database.LocalStorage` created on line ~67).

**dashboard/static/js/app.js** -- Add activity helpers and managed positions state:
- Add `managedPositions: []` to store initial state
- In `onmessage` handler, add: `if (data.managed_positions) this.managedPositions = data.managed_positions;`
- Add `formatTimeAgo(timestamp)` method: takes ISO timestamp string, returns human-readable relative time ("2s ago", "5m ago", "2h ago", "1d ago"). Use `Date.parse(timestamp)` and compute difference from `Date.now()`.
- Add `activityIcon(type)` method: returns emoji/symbol for activity types -- "POSITION_OPENED" -> a green up arrow character, "POSITION_CLOSED" -> a red down arrow, "ANALYSIS" -> magnifying glass, "INTELLIGENCE" -> lightbulb, "DECISION" -> brain. Use simple Unicode characters, not emoji (keeps it professional).
- Add `activityColor(type)` method: returns a Tailwind text color class string -- "POSITION_OPENED" -> "text-green-400", "POSITION_CLOSED" -> "text-red-400", "ANALYSIS" -> "text-blue-400", "INTELLIGENCE" -> "text-purple-400", "DECISION" -> "text-yellow-400", default -> "text-gray-400". Since these are returned as complete strings used in `:class`, Tailwind will find them in the JS source scan.

**dashboard/static/templates/dashboard.html** -- Replace placeholders with real panels:

Update the layout grid to: `grid grid-cols-1 lg:grid-cols-3 gap-4 p-4`. Portfolio overview in col 1, positions in cols 2-3 (first row). Activity feed spans cols 1-2, status panel in col 3 (second row).

**Activity Feed Panel** (replaces placeholder):
- Card: `bg-gray-900 rounded-lg p-4 lg:col-span-2`
- Title: "Activity Feed" with count badge
- Scrollable container: `max-h-96 overflow-y-auto space-y-2`
- Empty state when no activities
- Use `<template x-for="item in $store.dashboard.activities" :key="item.timestamp + item.action">` to iterate
- Each activity row: flex layout with icon (from `activityIcon`), content area (action text + symbol if present), timestamp (using `formatTimeAgo`), and optional details
- Color-code the action text using `:class="$store.dashboard.activityColor(item.type)"`
- Show the type as a small badge/label (uppercase, small text, muted color)
- If item has `symbol`, show it in mono font
- If item has `reasoning`, show as a truncated secondary line in gray-500 text (max 1 line with text-ellipsis overflow-hidden)

**Managed Positions Status** (within or below activity feed):
- If `$store.dashboard.managedPositions.length > 0`, show a small section listing managed positions with their status badges
- Status badges color-coded: ACTIVE -> green, PENDING -> yellow, STOPPED_OUT -> red, CLOSED -> gray, PARTIAL -> blue
- Use complete static Tailwind classes in conditionals, e.g., `:class="{ 'bg-green-500/20 text-green-400': pos.status === 'ACTIVE', 'bg-yellow-500/20 text-yellow-400': pos.status === 'PENDING', 'bg-red-500/20 text-red-400': pos.status === 'STOPPED_OUT', 'bg-gray-500/20 text-gray-400': pos.status === 'CLOSED' }"`

**Connection Status Indicator** (enhance existing header element):
- The connection dot + text from Plan 02 is already there. Enhance it:
- Add a pulsing animation to the green dot when live: `animate-pulse` class when status is "live"
- Show data age next to status text: "Live (2s)" / "Stale (15s)" / "Disconnected"
- Compute data age in a reactive way using a helper that reads from `$store.dashboard.lastUpdate`

**Bot Health Indicator** (replace placeholder in header):
- Show bot status next to connection status in header
- Green "Bot: Alive" with uptime when `$store.dashboard.botHealth.alive` is true
- Red "Bot: Dead" when alive is false
- Show uptime value from `$store.dashboard.botHealth.uptime`
- Small text showing last activity time using `formatTimeAgo`

**Tailwind CSS rebuild:**
- Run `./tools/tailwindcss -i dashboard/static/css/input.css -o dashboard/static/css/output.css --minify` (using CLI from Plan 02's tools/ directory)
- If tools/tailwindcss doesn't exist, re-download it

After HTML/JS changes, rebuild and verify `go build ./cmd/bot/` still compiles.
  </action>
  <verify>
Run `cd /home/dan/Coding/claude-prophet && go build ./cmd/bot/` -- compiles with all changes.
Run `cd /home/dan/Coding/claude-prophet && go vet ./...` -- no issues.
Verify activity feed in template: `grep -c 'activities' dashboard/static/templates/dashboard.html` should be >= 3.
Verify bot health in template: `grep -c 'botHealth' dashboard/static/templates/dashboard.html` should be >= 2.
Verify connection status in template: `grep -c 'connectionStatus' dashboard/static/templates/dashboard.html` should be >= 2.
Verify formatTimeAgo in app.js: `grep 'formatTimeAgo' dashboard/static/js/app.js`
Verify no dynamic Tailwind classes: `grep -c "text-\${" dashboard/static/templates/dashboard.html dashboard/static/js/app.js` should be 0.
Verify output.css rebuilt: `wc -l dashboard/static/css/output.css` should be > 0.
  </verify>
  <done>
Activity feed panel shows recent activities in reverse-chronological order with type icons, color-coding, symbols, timestamps, and reasoning excerpts. Managed position statuses displayed with color-coded badges. Connection status indicator shows live/stale/disconnected with data age. Bot health shows alive/dead with uptime. All data updates live via WebSocket. Tailwind CSS recompiled. Everything compiles.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete Phase 1 dashboard</name>
  <files>
    dashboard/static/templates/dashboard.html
  </files>
  <action>
Human visually verifies the complete Phase 1 dashboard end-to-end. This is a checkpoint -- no automated changes, only verification of what was built in Task 1 and prior plans.

What was built: Complete Phase 1 dashboard with all panels -- portfolio overview (cash/equity/buying power), open positions (with P&L color-coding and aggregate), activity feed (reverse-chronological with live updates), connection status indicator (green/yellow/red), and bot health indicator (alive/dead with uptime). All data pushed via WebSocket from the Go backend.

Steps to verify:
1. Start the bot: `cd /home/dan/Coding/claude-prophet && go run ./cmd/bot/`
2. Open browser to `http://localhost:4534/dashboard`
3. Verify the following:
   - Dark-themed command center layout loads with multiple panels
   - Connection status shows green "Live" dot (may take 2 seconds for first snapshot)
   - Bot health shows "Alive" with uptime counting up
   - Portfolio Overview panel shows cash, equity, and buying power values
   - Open Positions panel shows current positions (if any) with symbol, qty, entry price, current price, unrealized P&L
   - P&L values are green for profit, red for loss
   - Aggregate P&L shows at bottom of positions panel
   - Activity Feed shows recent activities (if bot has an active session)
   - Data updates every ~2 seconds without page refresh
4. Test reconnection: stop the bot (Ctrl+C), observe status turns red "Disconnected", restart bot, observe it reconnects and turns green again
5. Check browser console for any JavaScript errors

Resume signal: Type "approved" to complete Phase 1, or describe any issues to fix.
  </action>
  <verify>
Human confirms all five Phase 1 success criteria are met:
1. Multi-panel command center layout renders at /dashboard
2. Account balance, equity, buying power update in real-time
3. Open positions listed with P&L color-coded green/red, plus aggregate
4. Activity feed shows reverse-chronological updates live via WebSocket
5. Connection status (green/yellow/red) and bot health (alive/dead) visible
  </verify>
  <done>
Human has typed "approved" confirming the dashboard works correctly end-to-end, or issues have been identified and addressed.
  </done>
</task>

</tasks>

<verification>
Phase 1 complete when:
1. All five success criteria from ROADMAP.md are met:
   - User navigates to /dashboard and sees multi-panel command center layout
   - Account balance, equity, buying power update in real-time
   - Open positions listed with P&L color-coded green/red, plus aggregate
   - Activity feed shows reverse-chronological updates live via WebSocket
   - Connection status (green/yellow/red) and bot health (alive/dead) visible
2. `go build ./cmd/bot/` compiles
3. `go vet ./...` passes
4. Human visual verification approved
</verification>

<success_criteria>
- Activity feed displays recent activities with type icons, color-coding, and timestamps
- Position state changes (PENDING -> ACTIVE -> STOPPED_OUT etc.) visible in feed
- Connection status indicator degrades green -> yellow -> red based on data staleness
- Bot health shows alive/dead with uptime and last activity timestamp
- All panels update via WebSocket push without manual refresh
- Human has visually verified the dashboard works end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-core-dashboard/01-03-SUMMARY.md`
</output>
